-----------------------------------------------
Usage: panda-cluster COMMAND [arg...]
-----------------------------------------------
Follow any command with "help" for more information.

A tool to manage CoreOS clusters and deploy them on AWS with your account.
Panda-Cluster is built around a Cluster-App-Microservice (CAM) model for deployment, 
allowing it to understand and gracefully manipulate clusters running in your AWS account.

The following top-level commands access a specific component of that model.  They
are each followed by their own set of sub-commands to manipulate the component.

Commands:
   cluster    This is a set of CoreOS machines that stand ready to accept your applications.
   app        This is the application you wish to launch onto the cluster.
