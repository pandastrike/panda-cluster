-----------------------------------------------
Usage: pandacluster COMMAND [arg...]
-----------------------------------------------
Follow any command with "help" for more information.

A tool to manage CoreOS clusters and deploy them on AWS with your account.

Commands:
   create    Spins up a cluster of the requested size.
   destroy   Terminates the specified cluster.
